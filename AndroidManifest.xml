<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="org.grumpysoft"
      android:versionCode="1"
      android:versionName="1.0"
      android:installLocation="preferExternal">
    <uses-sdk android:minSdkVersion="15"
              android:targetSdkVersion="24"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application android:label="@string/app_name" android:icon="@drawable/icon_small">
        <activity android:name="ShowTrainsActivity" android:theme="@android:style/Theme.Holo"/>
        <activity android:name="ShowDetailsActivity" android:theme="@android:style/Theme.Holo"/>
        <activity android:name="ViewFlippingActivity" android:theme="@android:style/Theme.Holo"/>
        <activity android:name="NavigatorActivity" android:theme="@android:style/Theme.Holo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
